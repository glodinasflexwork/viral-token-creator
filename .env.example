# Viral Token Creator - Environment Variables
# Copy this file to .env and update the values as needed

# API Configuration (optional - defaults work for most cases)
# FLASK_ENV=development
# FLASK_DEBUG=true

# Solana Network Configuration (optional - handled by frontend)
# DEFAULT_NETWORK=devnet

# Custom RPC Endpoints (optional - uses public endpoints by default)
# SOLANA_DEVNET_RPC=https://api.devnet.solana.com
# SOLANA_MAINNET_RPC=https://api.mainnet-beta.solana.com

# Note: No environment variables are required for basic functionality
# The application works out of the box with sensible defaults

